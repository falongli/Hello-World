赋值为引用赋值，定义的值本身可以当成一个变量；
切片核心内容 [start:end:step]
三元运算符：为真时的结果 if 判定条件 else 为假时的结果  

元组不可改变；
列表和元组都可以切片访问；
元组切片依然是元组；

每个模块都具有属性__name__，如果模块直接运行，则__name__="__main__",如果通过import导入，该属性一般为文件名

